import Scanner;
import ArrayList;
// will not be changed:
import android.app.Activity;
import BullshitClassThatDoesNotExist;
// will remove trailing semicolons:
import Bundle;;;;
// will add additional commented match when -a option specified:
import OnClickListener;
// will add missing semicolon:
import Toast

public class TestJava {
    // Everything which does not start with "import" is preserved
    // That implies that imports done on indented lines are preserved as well:
    import ArrayList;
}